<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.martin.try4">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/HS"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WorkingEdit"
            android:configChanges="orientation|screenSize|keyboard"
            android:label="@string/Add_profile" />
        <activity
            android:name=".SingleProfile"
            android:label="@string/SP" />
        <activity android:name=".ColorPicker_try1" />
        <activity android:name=".PairDevice" />
        <activity android:name=".BTrecieve" />
    </application>

</manifest>
